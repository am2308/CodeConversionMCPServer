apiVersion: v1
kind: Service
metadata:
  name: mcp-converter-service
  namespace: mcp-converter
  labels:
    app: mcp-converter
spec:
  selector:
    app: mcp-converter
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    name: http
  type: ClusterIP